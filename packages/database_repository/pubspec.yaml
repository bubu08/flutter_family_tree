name: database_repository
description: Dart package which manages database queries.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  firebase_core: ^2.32.0
  meta: ^1.9.1
  cloud_firestore: ^4.17.5
  authentication_repository:
    path: ../authentication_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
